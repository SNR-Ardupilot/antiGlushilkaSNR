# 🚀 НАЧНИ ЗДЕСЬ - Запуск и тестирование

## ✅ Что сделать ПРЯМО СЕЙЧАС

### Шаг 1: Подготовьте Telegram бота (5 минут)

#### 1.1 Создайте бота

1. Откройте Telegram на телефоне или компьютере
2. Найдите бота **@BotFather**
3. Отправьте команду: `/newbot`
4. BotFather спросит имя бота, напишите: `My Test VPN Bot`
5. Затем спросит username, напишите: `my_test_vpn_bot` (должен быть уникальным)
6. **СКОПИРУЙТЕ токен!** Он выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

#### 1.2 Узнайте свой Telegram ID

1. Найдите бота **@userinfobot** в Telegram
2. Отправьте `/start`
3. Бот отправит ваши данные
4. **СКОПИРУЙТЕ число из строки "Id:"** (например, `123456789`)

### Шаг 2: Запустите проект локально (10 минут)

Откройте терминал (Terminal.app на Mac) и выполните команды:

```bash
# 1. Перейдите в директорию проекта
cd ~/yandex-vless-vpn

# 2. Убедитесь что Docker Desktop запущен
# Если не установлен, скачайте: https://www.docker.com/products/docker-desktop/
open -a Docker

# Подождите пока Docker запустится (значок кита в строке меню должен быть активным)

# 3. Запустите тестовый скрипт
./scripts/test_local.sh
```

### Шаг 3: Введите данные

Скрипт запросит:

**Telegram Bot Token:**
```
Введите Telegram Bot Token: [ВСТАВЬТЕ токен от BotFather]
```

**Telegram ID:**
```
Введите ваш Telegram ID: [ВСТАВЬТЕ ваш ID от userinfobot]
```

### Шаг 4: Дождитесь запуска

Скрипт автоматически:
- ✅ Проверит Docker
- ✅ Сгенерирует Reality ключи
- ✅ Создаст конфигурации
- ✅ Соберет Docker образы (займет 2-5 минут)
- ✅ Запустит контейнеры
- ✅ Проверит health статус

**Вы увидите:**
```
╔═══════════════════════════════════════════╗
║         ✅ Запуск успешен!                ║
╚═══════════════════════════════════════════╝

📱 Следующие шаги:
  1. Откройте Telegram
  2. Найдите вашего бота
  3. Отправьте команду: /start
  4. Нажмите кнопку '🔑 Получить конфигурацию'
```

### Шаг 5: Протестируйте бота

1. **Откройте Telegram**
2. **Найдите вашего бота** (по username который создали, например @my_test_vpn_bot)
3. **Нажмите "START"** или отправьте `/start`

Вы должны увидеть:
```
👋 Добро пожаловать в VLESS VPN бот!

🔐 Протокол: VLESS + Reality
🎭 Маскировка: Yandex.ru
🚀 Высокая скорость и безопасность

📍 Выберите действие:
```

С кнопками:
- 🔑 Получить конфигурацию
- 📱 Инструкция
- 👥 Админ-панель (только для вас)

4. **Нажмите "🔑 Получить конфигурацию"**

Бот должен:
- Создать конфигурацию для вас
- Отправить QR код
- Отправить VLESS ссылку

**Это значит что все работает! ✅**

---

## 📊 Проверка работы

### Просмотр логов

Откройте **новое окно терминала** и выполните:

```bash
cd ~/yandex-vless-vpn

# Просмотр логов в реальном времени
docker-compose logs -f

# Или отдельно для каждого сервиса:
docker-compose logs -f xray          # Логи VPN сервера
docker-compose logs -f telegram-bot  # Логи Telegram бота
```

### Проверка статуса

```bash
cd ~/yandex-vless-vpn

# Статус контейнеров
docker-compose ps

# Должны увидеть:
# NAME          STATUS         PORTS
# vless-xray    Up (healthy)   0.0.0.0:443->443/tcp
# vless-bot     Up (healthy)
```

### Мониторинг

```bash
cd ~/yandex-vless-vpn

# Запустить скрипт мониторинга
./scripts/monitor.sh
```

---

## 🛑 Остановка

Когда закончите тестирование:

```bash
cd ~/yandex-vless-vpn

# Остановить все контейнеры
docker-compose down

# Или остановить с удалением данных
docker-compose down -v
```

---

## 🐛 Если что-то пошло не так

### Проблема 1: Docker не запущен

**Решение:**
```bash
# Запустите Docker Desktop
open -a Docker

# Подождите 30 секунд и попробуйте снова
```

### Проблема 2: Бот не отвечает

**Решение:**
```bash
# Проверьте токен в .env
cat .env | grep TELEGRAM_BOT_TOKEN

# Если токен неправильный, отредактируйте .env:
nano .env
# Исправьте TELEGRAM_BOT_TOKEN
# Сохраните: Ctrl+X, затем Y, затем Enter

# Перезапустите бота
docker-compose restart telegram-bot

# Проверьте логи
docker-compose logs telegram-bot
```

### Проблема 3: Контейнер не запускается

**Решение:**
```bash
# Посмотрите логи
docker-compose logs

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d --force-recreate

# Проверьте статус
docker-compose ps
```

### Проблема 4: Порт 443 занят

**Решение:**
```bash
# Проверьте что использует порт 443
sudo lsof -i :443

# Если там что-то есть, остановите:
# Например, если это nginx:
# sudo systemctl stop nginx
```

---

## ✅ Чеклист успешного запуска

- [ ] Docker Desktop установлен и запущен
- [ ] Создал бота через @BotFather
- [ ] Получил свой Telegram ID через @userinfobot
- [ ] Запустил `./scripts/test_local.sh`
- [ ] Ввел Bot Token и Telegram ID
- [ ] Дождался сообщения "✅ Запуск успешен!"
- [ ] Открыл бота в Telegram
- [ ] Бот ответил на `/start`
- [ ] Получил конфигурацию и QR код
- [ ] `docker-compose ps` показывает "Up (healthy)"

**Если все пункты выполнены - проект работает!** 🎉

---

## 🌐 Следующий шаг: Деплой на сервер

После успешного локального тестирования можете развернуть на VPS:

1. Купите VPS (DigitalOcean, Vultr, Hetzner)
2. Подключитесь: `ssh root@YOUR_SERVER_IP`
3. Клонируйте проект: `git clone ...`
4. Запустите: `./scripts/deploy.sh`

Подробнее в **QUICKSTART.md** и **DOCKER_DEPLOY.md**

---

## 📚 Документация

- **QUICKSTART.md** - Быстрый старт (короткая версия)
- **TESTING_GUIDE.md** - Подробное руководство по тестированию
- **DOCKER_DEPLOY.md** - Полный гайд по Docker
- **README.md** - Общая документация проекта

---

## 🆘 Нужна помощь?

Если что-то не работает:

1. Проверьте логи: `docker-compose logs`
2. Проверьте статус: `docker-compose ps`
3. Посмотрите TESTING_GUIDE.md раздел "Troubleshooting"
4. Создайте issue в репозитории

---

**Удачи! Начинайте с запуска `./scripts/test_local.sh`** 🚀
