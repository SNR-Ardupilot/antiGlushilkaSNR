# ⚡ Быстрый старт

## 🏠 Локальное тестирование (на вашем Mac)

### 1️⃣ Подготовка Telegram бота

Перед запуском нужен Bot Token:

1. Откройте Telegram
2. Найдите **@BotFather**
3. Отправьте `/newbot`
4. Придумайте имя бота (например: `My VLESS VPN Bot`)
5. Придумайте username (например: `myvlessvpn_bot`)
6. **Скопируйте токен** (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

Также узнайте свой Telegram ID:
1. Найдите **@userinfobot**
2. Отправьте `/start`
3. **Скопируйте ваш ID** (число, например `123456789`)

### 2️⃣ Запуск проекта

Откройте терминал и выполните:

```bash
# Перейдите в директорию проекта
cd ~/yandex-vless-vpn

# Запустите тестовый скрипт
chmod +x scripts/test_local.sh
./scripts/test_local.sh
```

Скрипт спросит:
- **Bot Token** - вставьте токен от BotFather
- **Telegram ID** - вставьте ваш ID

После этого скрипт автоматически:
- ✅ Проверит Docker
- ✅ Сгенерирует Reality ключи
- ✅ Создаст конфигурации
- ✅ Соберет Docker образы
- ✅ Запустит контейнеры

### 3️⃣ Тестирование бота

После успешного запуска:

1. Откройте Telegram
2. Найдите вашего бота (по username который создали)
3. Отправьте `/start`
4. Нажмите кнопку **"🔑 Получить конфигурацию"**
5. Бот создаст конфигурацию и отправит QR код

**Ожидаемый результат:**

```
👋 Добро пожаловать в VLESS VPN бот!

🔐 Протокол: VLESS + Reality
🎭 Маскировка: Yandex.ru
🚀 Высокая скорость и безопасность
```

### 4️⃣ Просмотр логов

```bash
# Все логи в реальном времени
docker-compose logs -f

# Только логи бота
docker-compose logs -f telegram-bot

# Только логи Xray
docker-compose logs -f xray
```

### 5️⃣ Остановка

```bash
# Остановить все контейнеры
docker-compose down

# Остановить и удалить данные
docker-compose down -v
```

---

## 🌐 Деплой на VPS сервер

### 1️⃣ Подготовка

Вам нужен VPS с:
- Ubuntu 20.04+
- 1GB RAM
- Root доступ

**Где купить VPS:**
- [DigitalOcean](https://www.digitalocean.com/) - $6/мес
- [Vultr](https://www.vultr.com/) - $5/мес
- [Hetzner](https://www.hetzner.com/) - €4/мес

### 2️⃣ Подключение и установка

```bash
# Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# Обновите систему
apt update && apt upgrade -y

# Установите git
apt install -y git

# Клонируйте проект
git clone https://github.com/yourusername/yandex-vless-vpn.git
cd yandex-vless-vpn

# Запустите автодеплой
chmod +x scripts/deploy.sh
./scripts/deploy.sh
```

Скрипт запросит:
- **Bot Token** (от @BotFather)
- **Admin IDs** (ваш Telegram ID)

Все остальное произойдет автоматически!

### 3️⃣ Проверка

```bash
# Статус сервисов
docker-compose ps

# Логи
docker-compose logs -f

# Мониторинг
./scripts/monitor.sh
```

### 4️⃣ Использование

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Получите конфигурацию
4. Подключитесь через клиент (v2rayNG, Shadowrocket и т.д.)

---

## 📱 Клиенты для подключения

### Android
- **v2rayNG** (бесплатно) - [Google Play](https://play.google.com/store/apps/details?id=com.v2ray.ang)

### iOS
- **Shadowrocket** ($2.99) - App Store

### Windows
- **v2rayN** (бесплатно) - [GitHub](https://github.com/2dust/v2rayN/releases)

### macOS
- **v2rayU** (бесплатно) - [GitHub](https://github.com/yanue/V2rayU/releases)

---

## 🆘 Проблемы?

### Docker не установлен

**Mac:**
```bash
# Скачайте Docker Desktop
open https://www.docker.com/products/docker-desktop/
```

### Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d --force-recreate
```

### Бот не отвечает

```bash
# Проверьте что токен правильный
cat .env | grep TELEGRAM_BOT_TOKEN

# Перезапустите бота
docker-compose restart telegram-bot

# Проверьте логи
docker-compose logs telegram-bot
```

### Не могу подключиться к VPN

```bash
# На сервере проверьте firewall
ufw allow 443/tcp

# Проверьте что Xray запущен
docker-compose ps

# Проверьте порт
netstat -tulpn | grep 443
```

---

## 📚 Документация

- **README.md** - Полная документация проекта
- **DOCKER_DEPLOY.md** - Подробный гайд по Docker
- **TESTING_GUIDE.md** - Полное руководство по тестированию
- **bot/README.md** - Документация по боту

---

## 💡 Полезные команды

```bash
# Управление
docker-compose up -d          # Запуск
docker-compose down           # Остановка
docker-compose restart        # Перезапуск
docker-compose ps             # Статус

# Логи
docker-compose logs -f        # Все логи
docker-compose logs xray      # Только Xray
docker-compose logs telegram-bot  # Только бот

# Мониторинг
./scripts/monitor.sh          # Полный мониторинг
docker stats                  # Использование ресурсов
```

---

**Начните с локального тестирования, убедитесь что все работает, затем деплойте на сервер!**
