# 🧪 Руководство по тестированию VLESS VPN

## Локальное тестирование (Mac/Linux)

### Шаг 1: Подготовка окружения

```bash
# Перейдите в директорию проекта
cd ~/yandex-vless-vpn

# Убедитесь что Docker установлен
docker --version
docker-compose --version

# Если Docker не установлен:
# Mac: Скачайте Docker Desktop с https://www.docker.com/products/docker-desktop/
```

### Шаг 2: Создание тестового .env файла

```bash
# Создайте .env файл для тестирования
cat > .env <<'EOF'
# Telegram Bot Configuration (используйте реальный токен от @BotFather)
TELEGRAM_BOT_TOKEN=your_test_bot_token
ADMIN_IDS=your_telegram_id

# Debug mode
DEBUG=true

# Server Configuration (будет сгенерировано автоматически)
SERVER_IP=127.0.0.1
PUBLIC_KEY=test_public_key
PRIVATE_KEY=test_private_key
EOF

# Установите правильные права
chmod 600 .env
```

### Шаг 3: Генерация тестовых Reality ключей

```bash
# Создадим временный контейнер для генерации ключей
docker run --rm alpine:3.19 sh -c '
  apk add --no-cache curl unzip > /dev/null 2>&1
  curl -sL https://github.com/XTLS/Xray-core/releases/download/v1.8.7/Xray-linux-64.zip -o /tmp/xray.zip
  unzip -q /tmp/xray.zip -d /tmp
  /tmp/xray x25519
'

# Скопируйте полученные ключи в .env файл
```

### Шаг 4: Обновление конфигурации сервера

```bash
# Откройте server/config.json и обновите privateKey
# Вставьте ваш сгенерированный Private Key

# Или используйте sed (замените YOUR_PRIVATE_KEY на реальный ключ):
sed -i '' 's/"privateKey": ""/"privateKey": "YOUR_PRIVATE_KEY"/' server/config.json
```

### Шаг 5: Сборка Docker образов

```bash
# Соберите все образы
docker-compose build

# Проверьте что образы созданы
docker images | grep vless
```

### Шаг 6: Запуск контейнеров

```bash
# Запустите все сервисы
docker-compose up -d

# Проверьте статус
docker-compose ps

# Должны увидеть:
# NAME                COMMAND                  SERVICE             STATUS
# vless-xray          "/usr/local/bin/xray…"   xray                Up (healthy)
# vless-bot           "node dist/bot.js"       telegram-bot        Up (healthy)
```

### Шаг 7: Проверка логов

```bash
# Логи Xray сервера
docker-compose logs xray

# Логи Telegram бота
docker-compose logs telegram-bot

# Следить за логами в реальном времени
docker-compose logs -f
```

### Шаг 8: Тестирование Telegram бота

1. Откройте Telegram
2. Найдите вашего бота (тот для которого создали токен)
3. Отправьте `/start`
4. Должно появиться меню с кнопками

**Ожидаемый результат:**
```
👋 Добро пожаловать в VLESS VPN бот!

🔐 Протокол: VLESS + Reality
🎭 Маскировка: Yandex.ru
🚀 Высокая скорость и безопасность

📍 Выберите действие:
[🔑 Получить конфигурацию] [📱 Инструкция]
```

5. Нажмите "🔑 Получить конфигурацию"
6. Бот должен создать конфигурацию и отправить QR код

### Шаг 9: Проверка health checks

```bash
# Проверьте health статус контейнеров
docker inspect --format='{{.State.Health.Status}}' vless-xray
docker inspect --format='{{.State.Health.Status}}' vless-bot

# Оба должны показать: healthy
```

### Шаг 10: Мониторинг

```bash
# Запустите скрипт мониторинга
chmod +x scripts/monitor.sh
./scripts/monitor.sh
```

### Шаг 11: Остановка

```bash
# Остановить все сервисы
docker-compose down

# Остановить и удалить volumes
docker-compose down -v
```

---

## 🌐 Деплой на реальный VPS сервер

### Шаг 1: Подготовка VPS

Вам нужен VPS сервер с:
- Ubuntu 20.04+ / Debian 11+
- Минимум 1GB RAM
- Открытый порт 443
- Root доступ

**Рекомендуемые провайдеры:**
- DigitalOcean (от $6/мес)
- Vultr (от $5/мес)
- Hetzner (от €4/мес)
- Linode (от $5/мес)

### Шаг 2: Подключение к серверу

```bash
# Подключитесь к VPS
ssh root@YOUR_SERVER_IP

# Обновите систему
apt update && apt upgrade -y
```

### Шаг 3: Клонирование проекта

```bash
# Установите git (если нет)
apt install -y git

# Клонируйте репозиторий
git clone https://github.com/yourusername/yandex-vless-vpn.git
cd yandex-vless-vpn
```

### Шаг 4: Запуск автоматического деплоя

```bash
# Сделайте скрипт исполняемым
chmod +x scripts/deploy.sh

# Запустите деплой
./scripts/deploy.sh
```

Скрипт спросит:
1. **Telegram Bot Token** - получите у @BotFather в Telegram
2. **Admin IDs** - ваш Telegram ID (узнать у @userinfobot)

**Процесс деплоя:**
```
╔═══════════════════════════════════════════╗
║   VLESS + Reality VPN Secure Deploy      ║
╚═══════════════════════════════════════════╝

📦 Установка Docker...
📦 Установка Docker Compose...
📂 Рабочая директория: /root/yandex-vless-vpn
⚙️  Настройка переменных окружения...
🌐 IP сервера: 1.2.3.4
🔑 Генерация Reality ключей...
✅ Private Key: xxx
✅ Public Key: yyy

Введите Telegram Bot Token: [вводите токен]
Введите Telegram ID администраторов: [вводите ID]

✅ Файл .env создан и защищен
📝 Обновление конфигурации Xray...
🔒 Настройка firewall...
🛑 Остановка старых контейнеров...
🏗️  Сборка Docker образов...
🚀 Запуск сервисов...
⏳ Ожидание запуска сервисов...

📊 Статус сервисов:
NAME                COMMAND                  SERVICE             STATUS
vless-xray          "/usr/local/bin/xray…"   xray                Up (healthy)
vless-bot           "node dist/bot.js"       telegram-bot        Up (healthy)

✅ Деплой завершен успешно!

╔═══════════════════════════════════════════╗
║         Информация о развертывании        ║
╚═══════════════════════════════════════════╝

🌐 IP сервера: 1.2.3.4
🔑 Public Key: your_public_key
🔐 Private Key: your_private_key

📱 Telegram бот запущен и готов к работе
```

### Шаг 5: Проверка работы

```bash
# Проверьте статус
docker-compose ps

# Проверьте логи
docker-compose logs -f

# Запустите мониторинг
./scripts/monitor.sh
```

### Шаг 6: Тестирование подключения

1. **Откройте Telegram**, найдите бота
2. **Отправьте `/start`**
3. **Нажмите "🔑 Получить конфигурацию"**
4. **Скачайте QR код** или скопируйте VLESS ссылку

### Шаг 7: Подключение клиента

**Android:**
```
1. Установите v2rayNG из Google Play
2. Нажмите "+" → "Импорт из QR кода"
3. Отсканируйте QR код из Telegram
4. Нажмите на подключение
5. Включите VPN кнопкой внизу
```

**iOS:**
```
1. Купите Shadowrocket ($2.99)
2. Нажмите "+" → Scanner
3. Отсканируйте QR код
4. Включите переключатель
```

**Windows:**
```
1. Скачайте v2rayN
2. Скопируйте VLESS ссылку из бота
3. Нажмите Ctrl+V для импорта
4. Enter для подключения
```

---

## ✅ Чеклист проверки

### Сервер

- [ ] Docker контейнеры запущены (`docker-compose ps`)
- [ ] Health checks зеленые (healthy)
- [ ] Порт 443 открыт (`netstat -tulpn | grep 443`)
- [ ] Логи без ошибок (`docker-compose logs`)
- [ ] Firewall настроен (`ufw status`)

### Telegram бот

- [ ] Бот отвечает на `/start`
- [ ] Создается конфигурация для пользователя
- [ ] Генерируется QR код
- [ ] VLESS ссылка корректна
- [ ] Админ-панель доступна (для админов)

### VPN подключение

- [ ] Клиент импортирует конфигурацию
- [ ] Подключение устанавливается
- [ ] Интернет работает через VPN
- [ ] IP изменился (проверить на 2ip.ru)
- [ ] Скорость приемлемая

---

## 🐛 Troubleshooting

### Проблема: Контейнер не запускается

```bash
# Смотрим логи
docker-compose logs xray
docker-compose logs telegram-bot

# Пересоздаем контейнеры
docker-compose down
docker-compose up -d --force-recreate

# Проверяем ошибки сборки
docker-compose build --no-cache
```

### Проблема: Бот не отвечает

```bash
# Проверяем логи бота
docker-compose logs telegram-bot

# Проверяем токен в .env
cat .env | grep TELEGRAM_BOT_TOKEN

# Перезапускаем бота
docker-compose restart telegram-bot
```

### Проблема: Не могу подключиться к VPN

```bash
# Проверяем что Xray слушает 443
docker exec vless-xray netstat -tulpn | grep 443

# Проверяем firewall
ufw status

# Проверяем логи Xray на ошибки
docker-compose logs xray | grep -i error

# Проверяем что порт доступен извне
# На локальной машине:
telnet YOUR_SERVER_IP 443
```

### Проблема: Health check failed

```bash
# Проверяем процессы внутри контейнера
docker exec vless-xray ps aux
docker exec vless-bot ps aux

# Запускаем health check вручную
docker exec vless-xray sh -c "ps aux | grep xray | grep -v grep"
docker exec vless-bot sh -c "pgrep -f 'node.*bot.js'"
```

---

## 📊 Полезные команды

```bash
# Логи
docker-compose logs -f                    # Все логи
docker-compose logs -f xray              # Только Xray
docker-compose logs -f telegram-bot      # Только бот
docker-compose logs --tail=100           # Последние 100 строк

# Управление
docker-compose up -d                     # Запуск
docker-compose down                      # Остановка
docker-compose restart                   # Перезапуск
docker-compose restart xray              # Перезапуск Xray
docker-compose ps                        # Статус

# Мониторинг
./scripts/monitor.sh                     # Полный мониторинг
docker stats vless-xray vless-bot        # Ресурсы
docker-compose top                       # Процессы

# Отладка
docker exec -it vless-xray sh            # Войти в Xray контейнер
docker exec -it vless-bot sh             # Войти в бот контейнер
docker inspect vless-xray                # Полная информация
```

---

## 🎯 Демонстрация для университета

### Что показать:

1. **Архитектура**
   - Показать docker-compose.yml
   - Объяснить изоляцию сервисов
   - Показать security меры

2. **Маскировка трафика**
   - Показать config.json с Reality настройками
   - Объяснить как работает маскировка под Yandex
   - Показать что SNI = yandex.ru

3. **Telegram бот**
   - Демо работы бота
   - Показать код на TypeScript
   - Автоматическая выдача конфигов

4. **Безопасность**
   - Непривилегированные пользователи
   - Read-only файловые системы
   - Minimal capabilities
   - Health checks

5. **Deployment**
   - One-script deploy
   - Автоматическая генерация ключей
   - Мониторинг

---

## 📝 Тестовые сценарии

### Сценарий 1: Новый пользователь

1. Пользователь находит бота
2. Отправляет /start
3. Получает конфигурацию
4. Подключается к VPN
5. Проверяет IP (должен измениться)

### Сценарий 2: Администратор

1. Админ открывает бота
2. Видит кнопку "Админ-панель"
3. Смотрит статистику
4. Видит список пользователей

### Сценарий 3: Обновление

1. Остановка сервисов
2. Обновление кода
3. Пересборка образов
4. Запуск без простоя для пользователей

---

Готово! Следуйте этому гайду и все заработает. Начните с локального тестирования, а потом деплойте на сервер.
