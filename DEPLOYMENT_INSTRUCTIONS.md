# 🚀 Инструкция по развертыванию на VPS сервере

## Шаг 1: Подключение к серверу

Откройте терминал на вашем Mac и подключитесь:

```bash
# Замените YOUR_SERVER_IP на реальный IP вашего сервера
ssh root@YOUR_SERVER_IP
```

При первом подключении появится вопрос о fingerprint - введите `yes`.

Введите пароль от сервера (который получили на email).

**Вы подключены! Теперь вы работаете на сервере.**

---

## Шаг 2: Обновление системы

```bash
# Обновите список пакетов
apt update

# Обновите установленные пакеты
apt upgrade -y

# Установите необходимые утилиты
apt install -y git curl wget nano
```

---

## Шаг 3: Загрузка проекта

### Вариант A: Если проект в GitHub

```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/yandex-vless-vpn.git
cd yandex-vless-vpn
```

### Вариант B: Если проект НЕ в GitHub (загрузка с вашего Mac)

**На вашем Mac** (в новом окне терминала):

```bash
# Перейдите в директорию проекта
cd ~/yandex-vless-vpn

# Создайте архив (исключая ненужное)
tar czf vless-vpn.tar.gz \
  --exclude='node_modules' \
  --exclude='bot/node_modules' \
  --exclude='bot/dist' \
  --exclude='.git' \
  --exclude='*.log' \
  .

# Загрузите на сервер (замените YOUR_SERVER_IP)
scp vless-vpn.tar.gz root@YOUR_SERVER_IP:/root/

# Удалите локальный архив
rm vless-vpn.tar.gz
```

**Затем на сервере:**

```bash
# Распакуйте архив
cd /root
tar xzf vless-vpn.tar.gz -C yandex-vless-vpn
cd yandex-vless-vpn

# Удалите архив
rm /root/vless-vpn.tar.gz
```

---

## Шаг 4: Запуск автоматического деплоя

```bash
# Сделайте скрипт исполняемым
chmod +x scripts/deploy.sh

# Запустите деплой
./scripts/deploy.sh
```

### Что происходит:

Скрипт автоматически:
1. ✅ Установит Docker и Docker Compose
2. ✅ Сгенерирует Reality ключи (публичный и приватный)
3. ✅ Определит IP сервера
4. ✅ Запросит данные Telegram бота
5. ✅ Создаст конфигурации
6. ✅ Соберет Docker образы
7. ✅ Настроит firewall
8. ✅ Запустит все сервисы

### Вам нужно будет ввести:

**1. Telegram Bot Token:**
```
Введите токен Telegram бота: [ВСТАВЬТЕ ВАШ ТОКЕН]
```
(Тот же который использовали для локального тестирования)

**2. Admin IDs:**
```
Введите Telegram ID администраторов (через запятую): [ВАШ ID]
```
(Ваш Telegram ID: 1621380405)

### Процесс займет 5-10 минут

Вы увидите:
```
╔═══════════════════════════════════════════╗
║   VLESS + Reality VPN Secure Deploy      ║
╚═══════════════════════════════════════════╝

📦 Установка Docker...
📦 Установка Docker Compose...
🌐 IP сервера: 45.76.123.45
🔑 Генерация Reality ключей...
✅ Private Key: xxx...
✅ Public Key: yyy...
...
✅ Деплой завершен успешно!
```

---

## Шаг 5: Проверка работы

### Проверка статуса контейнеров:

```bash
docker-compose ps
```

Должны увидеть:
```
NAME         STATUS              PORTS
vless-xray   Up (healthy)        0.0.0.0:443->443/tcp
vless-bot    Up (healthy)
```

### Просмотр логов:

```bash
# Все логи
docker-compose logs -f

# Только Xray
docker-compose logs -f xray

# Только бот
docker-compose logs -f telegram-bot

# Для выхода нажмите Ctrl+C
```

### Проверка через мониторинг:

```bash
./scripts/monitor.sh
```

Выведет детальную информацию о:
- Статусе контейнеров
- Health checks
- Использовании ресурсов
- Активных подключениях
- Логах

---

## Шаг 6: Тестирование бота

1. **Откройте Telegram**
2. **Найдите вашего бота**
3. **Отправьте `/start`**
4. **Нажмите "🔑 Получить конфигурацию"**

Теперь бот будет выдавать реальные рабочие конфигурации с IP вашего сервера!

---

## Шаг 7: Подключение к VPN

### На Android (v2rayNG):

1. Установите v2rayNG из Google Play
2. Нажмите "+" → "Импорт из QR кода"
3. Отсканируйте QR код из Telegram бота
4. Нажмите на подключение
5. Разрешите создание VPN подключения
6. Нажмите кнопку подключения внизу

**Проверка:** Откройте 2ip.ru - должен показать IP вашего VPS сервера

### На iOS (Shadowrocket):

1. Купите Shadowrocket в App Store ($2.99)
2. Нажмите "+" → Scanner
3. Отсканируйте QR код
4. Включите переключатель
5. Подтвердите добавление VPN конфигурации

**Проверка:** Откройте 2ip.ru - должен показать IP вашего VPS сервера

---

## 🔧 Управление сервером

### Основные команды:

```bash
# Перезапуск всех сервисов
docker-compose restart

# Перезапуск только Xray
docker-compose restart xray

# Перезапуск только бота
docker-compose restart telegram-bot

# Остановка всех сервисов
docker-compose down

# Запуск всех сервисов
docker-compose up -d

# Просмотр использования ресурсов
docker stats

# Проверка логов
docker-compose logs --tail=100

# Очистка старых образов
docker system prune -a
```

### Мониторинг:

```bash
# Запуск скрипта мониторинга
./scripts/monitor.sh

# Добавление в cron для автоматической проверки каждые 5 минут
crontab -e

# Добавьте строку:
*/5 * * * * cd /root/yandex-vless-vpn && ./scripts/monitor.sh >> /var/log/vless-monitor.log 2>&1
```

---

## 🔒 Безопасность сервера

### Базовая настройка firewall:

```bash
# UFW уже настроен скриптом деплоя, но для справки:

# Проверка статуса
ufw status

# Должны быть открыты:
# - 22/tcp (SSH)
# - 443/tcp (VLESS)
```

### Изменение SSH порта (опционально, но рекомендуется):

```bash
# Откройте конфиг SSH
nano /etc/ssh/sshd_config

# Найдите строку:
# Port 22

# Измените на другой порт (например 2222):
Port 2222

# Сохраните: Ctrl+X, Y, Enter

# Откройте новый порт в firewall
ufw allow 2222/tcp

# Перезапустите SSH
systemctl restart sshd

# ВАЖНО: Не закрывайте текущую сессию!
# Откройте новое окно терминала и проверьте:
ssh -p 2222 root@YOUR_SERVER_IP

# Если работает, можете закрыть старую сессию
# Затем закройте старый порт:
ufw delete allow 22/tcp
```

### Автоматические обновления безопасности:

```bash
# Установка unattended-upgrades
apt install -y unattended-upgrades

# Включение автоматических обновлений
dpkg-reconfigure -plow unattended-upgrades
```

---

## 📊 Добавление новых пользователей

### Через Telegram бота (автоматически):

Каждый кто напишет `/start` боту - автоматически получит конфигурацию!

### Через командную строку:

```bash
# Добавить пользователя
./scripts/manage_users.sh add username

# Удалить пользователя
./scripts/manage_users.sh remove username

# Список всех пользователей
./scripts/manage_users.sh list

# Получить конфиг конкретного пользователя
./scripts/manage_users.sh get username
```

---

## 🆘 Troubleshooting

### Проблема: Контейнер не запускается

```bash
# Посмотрите логи
docker-compose logs xray
docker-compose logs telegram-bot

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d --force-recreate

# Проверьте использование ресурсов
free -h
df -h
```

### Проблема: Не могу подключиться к VPN

```bash
# Проверьте что Xray запущен
docker-compose ps

# Проверьте логи Xray
docker-compose logs xray | grep -i error

# Проверьте что порт 443 открыт
ufw status
netstat -tulpn | grep 443

# Проверьте что порт доступен извне (с вашего Mac)
telnet YOUR_SERVER_IP 443

# Проверьте конфигурацию
docker exec vless-xray cat /etc/xray/config.json
```

### Проблема: Бот не отвечает

```bash
# Проверьте логи бота
docker-compose logs telegram-bot

# Проверьте токен
cat .env | grep TELEGRAM_BOT_TOKEN

# Перезапустите бота
docker-compose restart telegram-bot

# Проверьте что бот запущен
docker exec vless-bot ps aux
```

### Проблема: Сервер перезагрузился

Docker контейнеры настроены на автоматический запуск (`restart: unless-stopped`), но если нужно:

```bash
# Перейдите в директорию проекта
cd /root/yandex-vless-vpn

# Запустите контейнеры
docker-compose up -d

# Проверьте статус
docker-compose ps
```

---

## 🔄 Обновление проекта

Если вы обновили код на GitHub:

```bash
# Перейдите в директорию проекта
cd /root/yandex-vless-vpn

# Получите обновления
git pull

# Пересоберите образы
docker-compose build

# Перезапустите с новыми образами
docker-compose up -d

# Проверьте что все работает
docker-compose ps
./scripts/monitor.sh
```

---

## 📈 Производительность

### Для большого количества пользователей:

1. **Увеличьте ресурсы VPS:**
   - 2GB RAM → 4GB RAM
   - 1 CPU → 2 CPU

2. **Настройте ограничения в docker-compose.yml:**

```yaml
services:
  xray:
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '1.0'
          memory: 1G
```

3. **Мониторьте нагрузку:**

```bash
# Использование ресурсов
htop

# Использование Docker контейнерами
docker stats

# Нагрузка на сеть
iftop
```

---

## 📝 Backup

### Бэкап конфигураций:

```bash
# Создайте директорию для бэкапов
mkdir -p ~/backups

# Сохраните важные файлы
cp .env ~/backups/
cp server/config.json ~/backups/

# Сохраните базу данных пользователей
docker run --rm -v yandex-vless-vpn_shared-data:/data -v ~/backups:/backup alpine \
  tar czf /backup/users-$(date +%Y%m%d).tar.gz -C /data .

# Скачайте бэкапы на ваш Mac
# (выполните на Mac, не на сервере):
# scp -r root@YOUR_SERVER_IP:~/backups ./server-backups
```

### Восстановление из бэкапа:

```bash
# Загрузите бэкап на сервер (с Mac):
# scp ./server-backups/users-20231031.tar.gz root@YOUR_SERVER_IP:/root/

# На сервере распакуйте:
docker run --rm -v yandex-vless-vpn_shared-data:/data -v ~/:/backup alpine \
  tar xzf /backup/users-20231031.tar.gz -C /data

# Перезапустите сервисы
docker-compose restart
```

---

## ✅ Чеклист успешного деплоя

- [ ] VPS сервер создан и доступен
- [ ] Подключились по SSH
- [ ] Система обновлена
- [ ] Проект загружен на сервер
- [ ] Скрипт deploy.sh выполнен успешно
- [ ] Оба контейнера запущены (healthy)
- [ ] Firewall настроен (порты 22, 443 открыты)
- [ ] Бот отвечает в Telegram
- [ ] Конфигурация генерируется
- [ ] VPN подключение работает
- [ ] IP меняется при подключении

---

## 🎓 Для презентации в университете

### Что показать:

1. **Архитектура:**
   - 2 Docker контейнера (Xray + Bot)
   - Изолированная сеть
   - Shared volume для данных

2. **Безопасность:**
   - Непривилегированные пользователи
   - Read-only файловые системы
   - Minimal capabilities
   - Reality протокол

3. **Маскировка:**
   - TLS handshake с Yandex
   - SNI = yandex.ru
   - Для DPI выглядит как обычный HTTPS

4. **Автоматизация:**
   - One-script deployment
   - Telegram бот для выдачи конфигов
   - Health checks
   - Автоматический перезапуск

5. **Демонстрация:**
   - Запрос конфига через бота
   - Подключение клиента
   - Проверка IP (до и после)
   - Логи сервера в реальном времени

---

**Готово! Следуйте этой инструкции пошагово и все заработает.** 🚀

При возникновении проблем - смотрите раздел Troubleshooting или пишите в чат.
